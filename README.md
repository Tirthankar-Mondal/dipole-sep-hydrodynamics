In dipole conserving system particularly I will look in aperiodic system of lattice with particles filled(1) or empty (0) and at only allowed transition are in active sites which are the type where (0110)<->(1001) is present. At each time step only one of the active site is randomly undergoes transition to the other configuration.
With this in mind if one sees the dynamics of this model one would find that some regions do not evolve at all. First I have to see that there is a finite fraction of such frozen region in the whole chain. Second I have to see that distribution of length of active sites whether they are exponential exp^(-l/eta) or some power law l^\alpha. If it is exponential then the active region are small clusters, otherwise in power law they can grow with the system size in thermodynamic limit. I want to see change in global density density correlation function with anomalous tail correction due to large active sites for this system if the active cluster can become large enough, and is modified by finite fraction of frozen sites in chain. Thus even tho the global transport is arrested due to large length there is a global relaxation of a perturbation. If this doesn't work we can find some other models where there might be power distribution of length of active sites.
