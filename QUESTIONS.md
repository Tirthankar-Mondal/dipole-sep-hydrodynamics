#1: See that if only a finite fraction of sites are frozen, get this for an equal density of 0,1 type particle see that if this fraction is fixed with different system size, eventually saturating at thermodynamic limit.

#2: See what is the length distribution of active sites that is the length of region between two frozen sites in PBC and see the Finite size effect of it.

#3: Active-region (gap) lengths of exactly 1, 2, 3, and 5 sites never occur between two frozen sites -- length 4 occurs (with an elevated count relative to its neighbors), and every length from 6 upward occurs, but 1, 2, 3, and 5 are completely absent across all L tested (100 to 12,800). Why?